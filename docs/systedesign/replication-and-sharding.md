# Репликация и шардирование

Репликация и шардирование — это два основных подхода к масштабированию баз данных. Они решают разные задачи и часто используются вместе.

---

## 🔁 1. Репликация (Replication)

### Что это?

Создание копий (реплик) одной и той же базы данных на других серверах. Все реплики содержат одни и те же данные.

### Основные типы

| Тип | Как работает | Использование |
|-----|--------------|---------------|
| **Master-Slave (Primary-Replica)** | Записи — только в master. Данные асинхронно/синхронно копируются на slave-реплики | Чтение с реплик (read scaling), резервное копирование, отказоустойчивость |
| **Multi-Primary** | Записи можно делать в любой узел | Редко, сложная согласованность |
| **Leaderless** (например, Cassandra) | Любая нода может принимать запись, согласованность через векторные часы | Высокая доступность, eventual consistency |

### ✅ Зачем нужно?

* **Масштабирование чтения**: если у вас 90% запросов — SELECT, вы можете направлять их на реплики
* **Отказоустойчивость**: если master падает — можно быстро переключиться на реплику (failover)
* **Географическое распределение**: реплика в Европе, реплика в Азии — пользователи получают данные быстрее

### ❌ Минусы

* **Задержка репликации (replication lag)**: реплика может быть «немного устаревшей» (актуально при асинхронной репликации)
* **Не масштабирует запись**: все INSERT/UPDATE/DELETE всё ещё идут в один master

### 🛠 Пример из контекста

В сервисе доставки еды:
* **Master** принимает новые заказы (запись)
* **3 read-реплики** отвечают за:
  * Отображение истории заказов
  * Поиск ресторанов (если данные хранятся в PostgreSQL с PostGIS)
  * Аналитику (например, «какие блюда популярны в Москве»)

Это снижает нагрузку на основную БД и повышает отказоустойчивость.

---

## 🧩 2. Шардирование (Sharding)

### Что это?

Горизонтальное разделение одной большой таблицы (или БД) на части (шарды), которые хранятся на разных серверах. Каждый шард содержит уникальный поднабор данных.

### Ключ шардирования (shard key)

Атрибут, по которому данные распределяются. Например:
* `user_id`
* `city_id`
* `order_date`

### Типы шардирования

| Тип | Как работает | Плюсы / Минусы |
|-----|--------------|----------------|
| **Range-based** | Шард 1: user_id 1–1M, Шард 2: 1M–2M… | Просто, но может быть неравномерная нагрузка |
| **Hash-based** | `hash(user_id) % N` → определяет шард | Равномерное распределение |
| **Directory-based** | Отдельный сервис хранит маппинг: user_id → шард | Гибко, но добавляет latency |

### ✅ Зачем нужно?

* **Масштабирование записи и хранения**: когда одна БД не справляется с объёмом данных или количеством транзакций
* **Снижение latency**: данные пользователя из Москвы хранятся в шарде в дата-центре Москвы
* **Изоляция нагрузки**: пик в Новосибирске не влияет на работу в Екатеринбурге

### ❌ Минусы

* **Сложность запросов**: нельзя просто сделать `SELECT * FROM orders` — нужно агрегировать данные со всех шардов
* **Нет транзакций между шардами**: нельзя атомарно обновить данные в двух шардах (без распределённых транзакций, которые медленные)
* **Сложность миграции и балансировки**: при добавлении нового шарда нужно перераспределять данные

### 🛠 Пример из контекста

У тебя 10 млн заказов в месяц, и растёт.
* Ты шардишь таблицу `orders` по `city_id`
* Москва → шард A
* Екатеринбург, Тюмень, Челябинск → шард B
* Владивосток → шард C

**Плюсы**:
* Запросы по Москве работают быстрее (меньше данных в шарде)
* Можно выделить больше ресурсов шарду Москвы (где больше заказов)
* При сбое в одном шарде — страдают только пользователи одного региона

---

## 🆚 Сравнение: репликация vs шардирование

| Характеристика | Репликация | Шардирование |
|----------------|------------|--------------|
| **Цель** | Надёжность + масштабирование чтения | Масштабирование записи + хранения |
| **Данные в узлах** | Одинаковые | Разные (части общего набора) |
| **Масштабирует запись?** | ❌ Нет | ✅ Да |
| **Сложность** | Низкая | Высокая |
| **Транзакции** | Полностью поддерживаются | Только внутри одного шарда |
| **Используется вместе?** | Часто — да! | Часто — да! |

💡 **На практике**: шарды реплицируются.  
Например:
* У тебя 3 шарда (по регионам)
* Каждый шард — это кластер из 1 master + 2 read-реплик  
→ Так ты получаешь и масштабируемость, и отказоустойчивость.

---

## 🧭 Когда что использовать?

### Используй репликацию, если:

* У тебя много чтений, мало записей
* Ты хочешь быстро сделать систему надёжнее
* Ты только растёшь, и одна БД ещё справляется с записью
* **Пример**: стартап с 10K пользователей, но 100K просмотров меню в день

### Используй шардирование, если:

* Твоя БД превышает лимиты одного сервера (по CPU, диску, памяти)
* Ты не можешь масштабировать запись даже на самом мощном сервере
* У тебя много данных, и они естественно делятся (по регионам, пользователям, датам)
* **Пример**: сервис доставки с 1 млн заказов/день по всей России

⚠️ **Шардирование — последнее средство**. Сначала попробуй:
* Оптимизировать запросы
* Добавить кэш (Redis)
* Использовать репликацию
* Перейти на более мощный сервер (вертикальное масштабирование)

---

## 💡 Совет для собеседования

На вопрос «Как вы масштабировали БД?» хорошая структура ответа:

> «Сначала мы добавили read-реплики, чтобы разгрузить основную БД от SELECT-запросов — это дало нам 3× рост в чтении. Когда нагрузка на запись достигла предела одного сервера, мы внедрили шардирование по city_id, так как наши данные естественно распределены по регионам. Каждый шард — это кластер из master + 2 реплик для отказоустойчивости. Шардирование позволило нам линейно масштабировать и хранение, и запись.»

---

## 📚 Связанные темы

* [Типы баз данных](./database-types.md) — выбор подходящего типа БД для системы
* [Масштабирование](./scaling.md) — вертикальное и горизонтальное масштабирование
* [Оценка нагрузки и ресурсов](./load-estimation.md) — как оценить, когда нужна репликация или шардирование
* [CAP теорема и консистентность](./cap-theorem.md) — как репликация влияет на консистентность
* [Load Balancing](./load-balancing.md) — балансировка запросов к репликам

