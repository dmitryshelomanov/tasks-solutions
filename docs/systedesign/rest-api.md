# REST API: принципы и примеры

REST (Representational State Transfer) — это архитектурный стиль для проектирования веб-сервисов. REST API — это интерфейс, который следует принципам REST и использует стандартные HTTP-методы для взаимодействия с ресурсами.

---

## 🔹 Основные принципы REST

### 1. Stateless (Без состояния)

Каждый запрос от клиента к серверу должен содержать всю необходимую информацию для его обработки. Сервер не хранит состояние клиента между запросами.

**Пример**:  
Вместо хранения сессии на сервере, каждый запрос содержит токен аутентификации в заголовке `Authorization`.

### 2. Ресурсы (Resources)

В REST всё является ресурсом, который идентифицируется через URI (Uniform Resource Identifier). Ресурсы представляют сущности системы.

**Примеры ресурсов**:
* `/users` — коллекция пользователей
* `/users/123` — конкретный пользователь с ID 123
* `/orders/456/items` — товары в заказе 456

### 3. HTTP-методы (Verbs)

Использование стандартных HTTP-методов для операций над ресурсами:

| Метод | Описание | Идемпотентность | Безопасность |
|-------|----------|-----------------|-------------|
| `GET` | Получить ресурс | ✅ Да | ✅ Да |
| `POST` | Создать новый ресурс | ❌ Нет | ❌ Нет |
| `PUT` | Обновить/создать ресурс (полная замена) | ✅ Да | ❌ Нет |
| `PATCH` | Частичное обновление ресурса | ❌ Нет | ❌ Нет |
| `DELETE` | Удалить ресурс | ✅ Да | ❌ Нет |

**Идемпотентность**: повторное выполнение операции даёт тот же результат.  
**Безопасность**: операция не изменяет состояние сервера.

### 4. Представления (Representations)

Ресурсы могут быть представлены в разных форматах (JSON, XML, HTML и т.д.). Клиент указывает предпочтительный формат через заголовок `Accept`.

**Пример**:
```
Accept: application/json
Accept: application/xml
```

### 5. Коды состояния HTTP (Status Codes)

Использование стандартных HTTP-кодов для индикации результата операции:

| Код | Категория | Примеры |
|-----|-----------|---------|
| `2xx` | Успех | `200 OK`, `201 Created`, `204 No Content` |
| `3xx` | Перенаправление | `301 Moved Permanently`, `304 Not Modified` |
| `4xx` | Ошибка клиента | `400 Bad Request`, `401 Unauthorized`, `404 Not Found` |
| `5xx` | Ошибка сервера | `500 Internal Server Error`, `503 Service Unavailable` |

---

## 🔹 Структура REST API

### Именование ресурсов

✅ **Хорошо**:
```
GET    /users
GET    /users/123
POST   /users
PUT    /users/123
DELETE /users/123

GET    /users/123/orders
POST   /users/123/orders
GET    /orders/456
```

❌ **Плохо**:
```
GET    /getUser?id=123
POST   /createUser
POST   /deleteUser/123
GET    /userOrders?userId=123
```

### Иерархия ресурсов

Ресурсы могут быть вложенными для отражения отношений:

```
/users/{userId}/orders/{orderId}/items/{itemId}
```

Но не стоит делать слишком глубокую вложенность (более 2-3 уровней). Альтернатива:

```
/orders/{orderId}/items/{itemId}  (если orderId уникален)
```

---

## 🔹 Примеры REST API

### Пример 1: Управление пользователями

#### Получить список пользователей

```http
GET /api/v1/users HTTP/1.1
Host: api.example.com
Accept: application/json
Authorization: Bearer {token}
```

**Ответ**:
```json
{
  "data": [
    {
      "id": 1,
      "name": "Иван Иванов",
      "email": "ivan@example.com",
      "created_at": "2024-01-15T10:30:00Z"
    },
    {
      "id": 2,
      "name": "Мария Петрова",
      "email": "maria@example.com",
      "created_at": "2024-01-16T14:20:00Z"
    }
  ],
  "pagination": {
    "page": 1,
    "per_page": 20,
    "total": 150
  }
}
```

#### Получить конкретного пользователя

```http
GET /api/v1/users/123 HTTP/1.1
Host: api.example.com
Accept: application/json
Authorization: Bearer {token}
```

**Ответ**:
```json
{
  "id": 123,
  "name": "Иван Иванов",
  "email": "ivan@example.com",
  "phone": "+7 999 123-45-67",
  "created_at": "2024-01-15T10:30:00Z",
  "updated_at": "2024-01-20T08:15:00Z"
}
```

#### Создать нового пользователя

```http
POST /api/v1/users HTTP/1.1
Host: api.example.com
Content-Type: application/json
Authorization: Bearer {token}

{
  "name": "Алексей Сидоров",
  "email": "alex@example.com",
  "phone": "+7 999 765-43-21"
}
```

**Ответ** (201 Created):
```json
{
  "id": 124,
  "name": "Алексей Сидоров",
  "email": "alex@example.com",
  "phone": "+7 999 765-43-21",
  "created_at": "2024-01-25T12:00:00Z"
}
```

#### Обновить пользователя (полная замена)

```http
PUT /api/v1/users/123 HTTP/1.1
Host: api.example.com
Content-Type: application/json
Authorization: Bearer {token}

{
  "name": "Иван Иванов",
  "email": "ivan.new@example.com",
  "phone": "+7 999 111-22-33"
}
```

**Ответ** (200 OK):
```json
{
  "id": 123,
  "name": "Иван Иванов",
  "email": "ivan.new@example.com",
  "phone": "+7 999 111-22-33",
  "updated_at": "2024-01-25T15:30:00Z"
}
```

#### Частичное обновление пользователя

```http
PATCH /api/v1/users/123 HTTP/1.1
Host: api.example.com
Content-Type: application/json
Authorization: Bearer {token}

{
  "phone": "+7 999 999-99-99"
}
```

**Ответ** (200 OK):
```json
{
  "id": 123,
  "name": "Иван Иванов",
  "email": "ivan.new@example.com",
  "phone": "+7 999 999-99-99",
  "updated_at": "2024-01-25T16:00:00Z"
}
```

#### Удалить пользователя

```http
DELETE /api/v1/users/123 HTTP/1.1
Host: api.example.com
Authorization: Bearer {token}
```

**Ответ** (204 No Content) — тело ответа отсутствует.

---

### Пример 2: Сервис доставки еды

#### Получить список ресторанов

```http
GET /api/v1/restaurants?city=moscow&limit=20 HTTP/1.1
Host: api.fooddelivery.com
Accept: application/json
```

**Ответ**:
```json
{
  "data": [
    {
      "id": 1,
      "name": "Пиццерия Марио",
      "cuisine": "Итальянская",
      "rating": 4.5,
      "delivery_time": "30-45 мин",
      "location": {
        "city": "Москва",
        "address": "ул. Тверская, 10"
      }
    }
  ],
  "meta": {
    "total": 150,
    "page": 1,
    "per_page": 20
  }
}
```

#### Получить меню ресторана

```http
GET /api/v1/restaurants/1/menu HTTP/1.1
Host: api.fooddelivery.com
Accept: application/json
```

**Ответ**:
```json
{
  "restaurant_id": 1,
  "restaurant_name": "Пиццерия Марио",
  "categories": [
    {
      "id": 1,
      "name": "Пицца",
      "dishes": [
        {
          "id": 101,
          "name": "Маргарита",
          "description": "Томаты, моцарелла, базилик",
          "price": 450,
          "image_url": "https://example.com/images/margarita.jpg"
        }
      ]
    }
  ]
}
```

#### Создать заказ

```http
POST /api/v1/orders HTTP/1.1
Host: api.fooddelivery.com
Content-Type: application/json
Authorization: Bearer {token}

{
  "restaurant_id": 1,
  "items": [
    {
      "dish_id": 101,
      "quantity": 2
    },
    {
      "dish_id": 205,
      "quantity": 1
    }
  ],
  "delivery_address": "ул. Ленина, 5, кв. 10",
  "payment_method": "card"
}
```

**Ответ** (201 Created):
```json
{
  "id": 789,
  "status": "pending",
  "restaurant_id": 1,
  "total_amount": 1250,
  "estimated_delivery_time": "2024-01-25T18:30:00Z",
  "created_at": "2024-01-25T17:45:00Z"
}
```

#### Получить статус заказа

```http
GET /api/v1/orders/789 HTTP/1.1
Host: api.fooddelivery.com
Accept: application/json
Authorization: Bearer {token}
```

**Ответ**:
```json
{
  "id": 789,
  "status": "in_progress",
  "restaurant_id": 1,
  "restaurant_name": "Пиццерия Марио",
  "items": [
    {
      "dish_id": 101,
      "dish_name": "Маргарита",
      "quantity": 2,
      "price": 450
    }
  ],
  "total_amount": 1250,
  "delivery_address": "ул. Ленина, 5, кв. 10",
  "estimated_delivery_time": "2024-01-25T18:30:00Z",
  "courier": {
    "name": "Алексей",
    "phone": "+7 999 123-45-67"
  },
  "tracking_url": "https://api.fooddelivery.com/tracking/789"
}
```

---

## 🔹 Версионирование API

Рекомендуется версионировать API для обратной совместимости:

```
/api/v1/users
/api/v2/users
```

Или через заголовок:
```
Accept: application/vnd.api+json;version=1
```

---

## 🔹 Пагинация

Для больших коллекций используйте пагинацию:

```
GET /api/v1/users?page=1&per_page=20
GET /api/v1/users?offset=0&limit=20
GET /api/v1/users?cursor=eyJpZCI6MTIzfQ
```

---

## 🔹 Фильтрация и сортировка

```
GET /api/v1/users?status=active&sort=created_at&order=desc
GET /api/v1/restaurants?city=moscow&cuisine=italian&min_rating=4.0
```

---

## 🔹 Обработка ошибок

### Стандартный формат ошибки

```json
{
  "error": {
    "code": "VALIDATION_ERROR",
    "message": "Email is required",
    "details": {
      "field": "email",
      "reason": "required"
    }
  }
}
```

### Примеры кодов ответов

* `400 Bad Request` — неверный формат запроса
* `401 Unauthorized` — требуется аутентификация
* `403 Forbidden` — недостаточно прав
* `404 Not Found` — ресурс не найден
* `409 Conflict` — конфликт (например, email уже используется)
* `422 Unprocessable Entity` — ошибка валидации
* `500 Internal Server Error` — внутренняя ошибка сервера

---

## ✅ Best Practices

1. **Используйте существительные, а не глаголы** в URI
2. **Используйте множественное число** для коллекций (`/users`, а не `/user`)
3. **Используйте иерархию** для связанных ресурсов (`/users/123/orders`)
4. **Версионируйте API** с самого начала
5. **Используйте правильные HTTP-методы** и коды состояния
6. **Документируйте API** (OpenAPI/Swagger)
7. **Используйте пагинацию** для больших коллекций
8. **Обрабатывайте ошибки** единообразно
9. **Используйте HTTPS** в production
10. **Реализуйте rate limiting** для защиты от злоупотреблений

---

## 📚 Связанные темы

* [Функциональные и нефункциональные требования](./functional-and-non-functional-requirements.md) — требования к API
* [Оценка нагрузки и ресурсов](./load-estimation.md) — оценка нагрузки на API
* [Load Balancing](./load-balancing.md) — балансировка нагрузки для API серверов
* [Стратегии кэширования](./caching-strategies.md) — кэширование ответов API
* [Пример системного собеседования](./system-design-interview-example.md) — примеры проектирования API

