# Масштабирование: вертикальное и горизонтальное

Масштабирование — это процесс увеличения производительности системы для обработки растущей нагрузки. Существует два основных подхода: вертикальное (Scale Up) и горизонтальное (Scale Out).

---

## 🔹 Вертикальное масштабирование (Vertical Scaling, «Scale Up»)

### Что это?

Увеличение вычислительных ресурсов одного сервера:
* Больше CPU
* Больше RAM
* Быстрее диск (NVMe вместо HDD)
* Выше пропускная способность сети

### Пример

У вас был сервер с 4 ядрами и 8 ГБ RAM → вы «апгрейдите» его до 16 ядер и 64 ГБ RAM.

### ✅ Преимущества

* **Простота**: ничего не нужно менять в архитектуре приложения
* **Нет необходимости в распределённой логике** (например, шардинге)
* **Подходит для монолитов и legacy-систем**

### ❌ Недостатки

* **Физический предел**: существует максимальная конфигурация сервера (например, 128 ядер, 4 ТБ RAM)
* **Single point of failure**: если сервер упадёт — упадёт вся система
* **Дорого**: мощные серверы («big iron») стоят экспоненциально дороже
* **Сложность апгрейда**: часто требует остановки сервиса (downtime)

### Когда использовать?

* На старте проекта (MVP)
* Для stateful-сервисов, где распределённость вредна (например, старые базы данных без репликации)
* Когда приложение не поддерживает распределённую архитектуру

💡 **Пример**: запуск PostgreSQL на одном очень мощном сервере — это вертикальное масштабирование.

---

## 🔹 Горизонтальное масштабирование (Horizontal Scaling, «Scale Out»)

### Что это?

Добавление новых серверов (нод) в кластер и распределение нагрузки между ними.

### Пример

У вас 1 сервер обрабатывает 1000 RPS → вы добавляете ещё 3 таких же → система обрабатывает ~4000 RPS.

### Для этого обычно используется:

* **Load Balancer** (Nginx, AWS ALB, HAProxy)
* **Stateless-архитектура** (сервисы не хранят данные локально)
* **Распределённое хранение** (шардинг, репликация)

### ✅ Преимущества

* **Практически неограниченная масштабируемость** — можно добавлять сотни/тысячи нод
* **Отказоустойчивость**: падение одного сервера — не критично
* **Эластичность**: легко масштабировать «на лету» (например, в облаке: AWS, GCP)
* **Экономия**: дешёвые commodity-серверы вместо одного дорогого

### ❌ Недостатки

* **Сложность архитектуры**: нужно решать проблемы согласованности, распределённых транзакций, маршрутизации
* **State management**: сессии, кэш, данные — нужно хранить централизованно (Redis, DB)
* **Может потребовать переписывания части логики** (например, отказ от in-memory кэша)

### Когда использовать?

* В production-системах с высокой нагрузкой (веб-приложения, API, микросервисы)
* Когда важна отказоустойчивость и uptime
* В облачных средах (Kubernetes, AWS ECS и т.п.)

💡 **Пример**: ваш бэкенд на Express.js запущен в 10 репликах в Kubernetes, перед ними — ingress-контроллер. Это горизонтальное масштабирование.

---

## 🆚 Сравнение в таблице

| Критерий | Вертикальное масштабирование | Горизонтальное масштабирование |
|----------|------------------------------|--------------------------------|
| **Как масштабировать** | Увеличить мощность одного сервера | Добавить больше серверов |
| **Архитектурная сложность** | Низкая | Высокая |
| **Максимальный предел** | Ограничен железом | Практически неограничен |
| **Отказоустойчивость** | Низкая (SPOF) | Высокая |
| **Стоимость** | Высокая (экспоненциальный рост) | Умеренная (линейный рост) |
| **Подходит для** | Монолитов, старых систем | Микросервисов, облачных приложений |

---

## 💡 Как это применяется на практике (и на собеседованиях)

* **Современные системы стремятся к горизонтальному масштабированию**, потому что:
  * Облака (AWS, GCP) идеально поддерживают его
  * Kubernetes, Docker, serverless — всё это «scale out»

* **Однако базы данных часто масштабируются вертикально** (особенно OLTP, например, PostgreSQL), пока это возможно, а потом переходят к репликации (read replicas) или шардингу — что уже форма горизонтального масштабирования.

* **На собеседовании важно уметь сказать**:
  > «Мы используем горизонтальное масштабирование для stateless-сервисов (через Kubernetes), а для базы данных — вертикальное масштабирование + read replicas, так как шардинг усложнит согласованность заказов.»

---

## 🧠 Запомни фразу-ассоциацию

> **«Scale Up = Bigger Machine. Scale Out = More Machines.»**

---

---

## Навигация

[← Предыдущая: CAP теорема и консистентность](./cap-theorem.md) | [Следующая: Load Balancing →](./load-balancing.md)

