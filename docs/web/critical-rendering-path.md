# Критический путь рендеринга (Critical Rendering Path, CRP)

**Критический путь рендеринга** — это последовательность шагов, которые браузер выполняет, чтобы превратить HTML/CSS/JS в пиксели на экране.

Оптимизация CRP нужна, чтобы **минимизировать время между запросом страницы и первым значимым отображением контента (FCP, LCP)** и избежать ситуации, когда пользователь долго видит **белый экран**.

---

## 🔑 Что входит в критический путь рендеринга

Упрощённо, браузер проходит такие шаги:

1. **Загрузка HTML**
2. **Парсинг HTML → построение DOM**
3. **Загрузка и парсинг CSS → построение CSSOM**
4. **Объединение DOM + CSSOM → Render Tree**
5. **Layout** — расчёт позиций и размеров элементов
6. **Paint / Composite** — отрисовка пикселей на экране

Пока не будут готовы **DOM** и **CSSOM**, браузер не может корректно отрисовать страницу.

Подробнее:
- **[Как работает браузер](./how-browsers-work.md)** — навигация, парсинг, дерево рендеринга, layout, paint
- **[JS-движки и браузерные движки](./js-and-browser-engines.md)** — движки и их взаимодействие

---

## 🎯 Цель оптимизации CRP

Цель — **ускорить отображение полезного контента**:

- сократить количество **блокирующих ресурсов** (CSS/JS, без которых браузер не может продолжать рендер),
- уменьшить **размер и сложность критического CSS/JS**,
- показать пользователю **основной контент как можно раньше**, даже если второстепенные части ещё грузятся.

Особенно это важно для:

- **мобильных устройств** (медленная сеть, слабый CPU),
- **SPA/React-приложений**, где без JS пользователь может долго видеть пустую страницу.

---

## 💡 Паттерны оптимизации CRP

Ниже — ключевые паттерны, часто используемые в современных приложениях (в том числе на React, Next.js и др.).

### 1. Server-Side Rendering (SSR)

- Рендеринг HTML **на сервере** (React/SSR, Next.js, Remix и др.).
- Браузер получает **готовый HTML**, может сразу построить DOM и отрисовать контент.
- JS-бандл загружается и **гидрирует** уже отрисованный HTML.

Плюсы:
- Быстрый **первый рендер** (FCP/LCP),
- Лучшее **SEO**, т.к. контент есть в HTML.

Минусы:
- Нагрузка на сервер,
- Сложнее кэширование и настройка инфраструктуры.

### 2. Streaming SSR

- HTML отправляется **потоком, частями**.
- Критические блоки (шапка, основной контент) приходят первыми,
- Остальные части — по мере готовности.

В React 18+ это делается через **`<Suspense>` + streaming SSR**.

Идея: **сократить время до первого «полезного» контента**, даже если некоторым блокам нужны данные/JS.

### 3. Progressive / Selective Hydration

- Гидратация (оживление HTML через JS) происходит **постепенно**, а не одним большим куском.
- Сначала гидрируются:
  - интерактивные элементы (кнопки, формы, поиск),
  - критические части интерфейса.
- Менее важные блоки (комментарии, виджеты, футер) — позже.

Это уменьшает **время до интерактивности (TTI)**, т.к. браузер не «подвешен» сразу большим количеством JS.

### 4. Code Splitting и Lazy Loading

- Разбиваем бандл на чанки:
  - динамические импорты: `import('./Component')`,
  - в React — `React.lazy(() => import('./Component'))`.
- Загружаем только тот код, который нужен **на текущем экране**.

Плюсы:

- Уменьшение размера **критического JS-бандла**,
- Отложенная загрузка тяжёлых страниц и фич.

### 5. Inlining критических ресурсов

- Встраиваем **критический CSS** прямо в HTML (в `<style>` в `<head>`),
- Иногда — небольшой критический JS (минимум, без лишней логики).

Задача:

- Убрать лишние сетевые запросы, без которых нельзя начать рендер,
- Оставить всё некритическое CSS в отдельных файлах и загружать их асинхронно.

### 6. Skeleton Screens / Placeholders

- Вместо пустого экрана показываются **скелетоны**:
  - серые блоки вместо текста/картинок,
  - простые контейнеры вместо реального контента.

Пользователь:

- Видит, **где будет контент и какого он типа**,
- Воспринимает интерфейс как более «живой» и быстрый.

### 7. App Shell Architecture

- Сначала рендерится **оболочка приложения (App Shell)**:
  - шапка,
  - навигация,
  - каркас страницы,
  - скелетоны.
- Данные и внутренний контент подгружаются **асинхронно**.

Часто используется в:

- **PWA**,
- мобильных и оффлайн-first приложениях.

---

## 🔍 Что считается «критическим»?

Критический контент — это то, что:

- Виден **без прокрутки** (above-the-fold),
- Важен для **цели страницы**:
  - заголовок,
  - ключевой текст,
  - CTA-кнопка (зарегистрироваться, купить, оформить),
  - цена/основное предложение,
- Важен для **SEO**:
  - заголовки (`<h1>`, `<h2>`),
  - текст, который описывает основной контент.

Всё остальное (комментарии, рекомендации, виджеты, анимации, второстепенные блоки) можно:

- загружать позже,
- рендерить после основного контента,
- гидрировать в фоновом режиме.

---

## 🧩 CRP и React / SPA

В чистых клиентских SPA:

- Браузер загружает **минимальный HTML** (часто почти пустой `div#root`),
- Потом грузит **JS-бандл**,
- Только после выполнения JS:
  - строится виртуальное дерево компонентов,
  - рендерится DOM,
  - пользователь видит хоть какой-то контент.

Именно поэтому:

- **SSR, streaming, code splitting, skeletons, App Shell** — критичны для хорошего UX и SEO.

Хорошая стратегия:

1. Быстро отдать **HTML с критическим контентом** (SSR/Streaming).
2. Встроить **критический CSS** для первого экрана.
3. Отложить:
   - тяжёлый JS,
   - неважные виджеты,
   - аналитику и второстепенные скрипты.

---

## 🧾 Итог

**Паттерны критического рендеринга** помогают:

- **сократить время до первого полезного рендера (FCP/LCP)**,
- **уменьшить время до интерактивности (TTI)**,
- сделать интерфейс **визуально быстрым**,
- улучшить **SEO и поведенческие метрики**.

Используйте их в связке:

- SSR / Streaming SSR,
- Progressive / Selective Hydration,
- Code Splitting + Lazy Loading,
- App Shell + Skeleton Screens,
- Inlining критического CSS.

Всё это опирается на понимание:

- **как работает браузер и движки** → см. [JS-движки и браузерные движки](./js-and-browser-engines.md),
- **как работает Event Loop и асинхронность** → см. [Event Loop в JavaScript](../javascript/event-loop.md).

