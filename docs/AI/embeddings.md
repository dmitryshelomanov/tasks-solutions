# Эмбеддинги (Embeddings)

## Что это?

**Эмбеддинги** (от англ. *embeddings*) — это числовой способ представления данных (чаще всего текста, но также изображений, аудио и т.д.) в виде векторов фиксированной длины, где семантически близкие объекты имеют близкие векторы в многомерном пространстве.

## 🔍 Простой пример

Представьте слова:
- «король» → `[0.8, -0.2, 0.5, ...]`
- «королева» → `[0.78, -0.22, 0.51, ...]`
- «автомобиль» → `[-0.1, 0.9, -0.3, ...]`

Вектора «король» и «королева» будут близки друг к другу, а оба — далеки от «автомобиля», потому что их смысл связан.

## 🎯 Зачем нужны эмбеддинги?

### 1. Превращают неструктурированные данные в числа

Компьютеры не понимают слова или картинки напрямую — только числа. Эмбеддинги дают способ кодировать смысл в формат, понятный ML-моделям.

### 2. Сохраняют семантику

Близость векторов ≈ близость смысла. Это позволяет:
- Находить похожие документы или товары
- Группировать (кластеризовать) тексты
- Определять тематику

### 3. Ускоряют и упрощают поиск

Вместо сравнения текстов по словам (медленно), можно сравнивать их векторы через **косинусное расстояние** или **евклидово расстояние** — быстро и эффективно.

### 4. Используются как вход для нейросетей

Например, LLM (вроде GPT) сначала превращают каждое слово в эмбеддинг, а потом обрабатывают последовательность векторов.

### 5. Позволяют делать RAG (Retrieval-Augmented Generation)

Чтобы LLM отвечал на вопросы по вашим документам:
- Ваши документы → эмбеддинги → сохраняются в векторной БД
- Запрос пользователя → эмбеддинг → поиск ближайших документов
- LLM получает релевантный контекст → даёт точный ответ

### 6. Поддерживают мультимодальность

Современные модели (например, **CLIP**) создают эмбеддинги и для текста, и для изображений в одном пространстве. Это позволяет искать картинки по тексту и наоборот.

## 🧠 Как создаются эмбеддинги?

### Готовые модели

- **text-embedding-ada-002** (OpenAI) — популярная модель для текстовых эмбеддингов
- **all-MiniLM-L6-v2** (Sentence Transformers) — лёгкая и быстрая модель
- **e5, BGE, DPR** — специализированные модели для разных задач

### Контекстные эмбеддинги

- **BERT, RoBERTa** — дают разные векторы для одного слова в зависимости от контекста
- Учитывают окружение слова в предложении

### Специализированные модели

- Для кода (CodeBERT, GraphCodeBERT)
- Для медицины
- Для права
- Для конкретных доменов

## 💡 Где применяются?

- **Поиск по семантике** (а не по ключевым словам)
- **Чат-боты с памятью о документах** (RAG)
- **Рекомендательные системы**
- **Классификация и кластеризация текстов**
- **Обнаружение дубликатов**
- **Перевод семантического сходства в числа** (например, «насколько два отзыва похожи?»)

## 📌 Итог

> Эмбеддинги — это «язык смысла» для машин: они превращают слова, изображения или другие данные в точки в пространстве, где расстояние = смысловое сходство. Без них современный ИИ (особенно LLM и поиск) просто не работал бы эффективно.

---

*См. также: [LLM](./llm.md), [MCP](./mcp.md)*

