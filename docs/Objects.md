# Обьекты


## Пример того что все обьекты ссылочные

> В примере видно что obj это просто ссылка на место в памяти и заменяя ссылку, мы никак не очищаем память

```js
let obj = { a: 1 };
let array = [obj];

obj = null;
console.log(array); // [ { a: 1 } ]
```