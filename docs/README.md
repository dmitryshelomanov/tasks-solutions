# Документация (VitePress)

Сайт документации собирается через VitePress и деплоится на GitHub Pages.

## Шаблон для index-страницы раздела

При добавлении или правке раздела придерживайтесь единой структуры `index.md`:

1. **Заголовок (H1)** — название раздела.
2. **Краткое описание** — 1–2 предложения, о чём раздел.
3. **Содержание** — блок `## Содержание` (или `## 📚 Содержание`) с подзаголовками по темам и ссылками на статьи.
4. **Связанные разделы** (по желанию) — блок `## 🔗 Связанные разделы` со ссылками на смежные разделы.
5. В конце при желании — ссылка «Вернуться к главной документации».

Для разделов с большим числом статей (например, системный дизайн) можно группировать содержание по подтемам (Основы, Базы данных, Масштабирование и т.д.).

## Добавление новой страницы

При добавлении нового `.md` файла в раздел:

1. **Обновить sidebar** в [docs/.vitepress/config.mts](.vitepress/config.mts): добавить пункт в массив `items` нужного раздела в `themeConfig.sidebar`. Иначе страница будет доступна по прямой ссылке, но не отобразится в боковом меню.
2. **Обновить index раздела** — добавить ссылку на новую статью в блок «Содержание» соответствующего `index.md`.
3. При необходимости добавить в статью frontmatter с `title` и `description` для SEO.

Запуск локально: `yarn dev:docs`. Сборка: `yarn build:docs`.
